import { css } from 'lit';

export class {COMPONENT_NAME}Theme {
  static cssBase = css`
    :host {
      display: block;
    }
  `;

  static {COMPONENT_NAME}Theme = [{COMPONENT_NAME}Theme.cssBase];
}
